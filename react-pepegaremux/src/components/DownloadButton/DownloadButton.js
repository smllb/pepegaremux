import Button from '@mui/material/Button';


const downloadAllVideosFromList = () => {
  
}

function DownloadButton() {
  return (
    <Button variant="contained" color="primary" onClick={downloadAllVideosFromList}>
      Download
    </Button>
  );
}

export default DownloadButton