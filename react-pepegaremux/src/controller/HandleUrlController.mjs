// import api from '../static/api.json' assert { type: 'json'}

// const BASE_URL = api.base.url+api.base.port;
// const ENDPOINTS = api.base.endpoints;

const handleUrlSubmit = (event) => {

}

const appraiseUrlFromRequest = async (url) => {
    
    
    
}

const checkAppraise = async (url) => {
    let result = await appraiseUrl(url)
    console.log(result)
}

